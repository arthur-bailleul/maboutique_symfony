<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom">
  <div class="container">

    <a class="navbar-brand fw-semibold" href="#">Navbar</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('home') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Pricing</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('products') }}">Produits</a>
        </li>
      </ul>


      <ul class="navbar-nav">
        <li class="nav-item d-flex align-items-center me-3">
            <a class="fa-solid fa-basket-shopping nav-link" href="{{ path('cart') }}" style="color: hsl(0, 0%, 15%);"></a>
        </li>
        {# 👉 Visible uniquement pour admin #}
        {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
          <a class="nav-link text-secondary-emphasis fw-semibold" href="{{ path('admin') }}">
            Dashboard
          </a>
        </li>
        {% endif %}

        {% if not app.user %}
        <li class="nav-item">
          <a class="nav-link" href="{{path('register')}}">Inscription</a>
        </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('login') }}">Connexion</a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{path('account')}}">Compte</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-danger" href="{{logout_path()}}">Déconnexion</a>
          </li>
        {% endif %}
      </ul>

    </div>
  </div>
</nav>
{# TODO: Ne rien mettre apres un dump sinon ca s'execute pas  #}
{# <pre> #}
    {# {{dump(app.flashes)}} #}
{# </pre> #}

{# <pre> #}
    {# {{dump(app)}} #}
{# </pre> #}

<div class="row justify-content-center">

    {% for label, messages in app.flashes %}
        <div class="alert alert-{{ label }} alert-dismissible fade show col-md-auto" role="alert">
          <strong>{{ label|upper }}!</strong>

                {% for message in messages %}
                <div class="flash-notice">
                    {{ message }}
                </div>
                {% endfor %}
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    {% endfor %}

</div>
